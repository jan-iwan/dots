#!/bin/bash

scripts=(
    autostart
    input
    keybindings
    decoration
)

for script in ${scripts[@]}
do
    riverctl spawn "$HOME/.config/river/scripts/$script" &
done

# layout
rivertile -view-padding 0 -outer-padding 0 -main-ratio 0.5
