#!/bin/bash

# This is a wrapper to run python code from a virtual environment

# Get path of virtual environment
BIN_DIR=$(dirname $0)
VENV_DIR="${BIN_DIR}/pyvenvs/ign"

# Activate the environment
source "${VENV_DIR}/.venv/bin/activate"

# Run the program
python "${VENV_DIR}/ign.py"
